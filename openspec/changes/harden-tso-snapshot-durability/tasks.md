## 1. Baseline and Test Seams

- [ ] 1.1 Build the unchanged target revision, run the current TSO/transaction CTest cases, and record commit, dirty-tree inventory, compiler, filesystem, durability mount options, CPU, segment size, concurrency, throughput, and p50/p95/p99 in `openspec/changes/harden-tso-snapshot-durability/evidence/baseline.md`; verify by attaching the exact build/test/benchmark commands and raw result paths, and do not edit or clean any pre-existing dirty source or the separate `docs/` worktree.
- [ ] 1.2 Add a test-only, dependency-injected Persister fault-stage interface for write, data sync, rename, manifest sync, and directory sync without production sleeps; verify with a small unit fake that observes every named stage in order, and limit edits to Persister/test/CMake files while preserving unrelated changes.
- [ ] 1.3 Add reusable subprocess, blackhole-socket, temporary-directory, and bounded-wall-clock helpers for reliability tests; verify each helper with a self-contained smoke case and timeout, and limit edits to `test/` support and its CMake registration.

## 2. Durable Persistence Implementation

- [ ] 2.1 Define the versioned little-endian envelope, CRC32C validation, storage identity, Raft/snapshot generation metadata, and structured `PersistedRaftBundle` result; verify encode/decode round trips for empty, binary, and maximum-length fields, and limit edits to `src/raft/include`, `src/raft`, and focused test files.
- [ ] 2.2 Replace truncating stream writes with same-directory temporary writes, complete-write loops, data sync, immutable generation rename, alternating manifest publication, directory sync, and two-generation retention under `Persister::m_mtx`; verify syscall-stage order through the injected fake and restart reads, and do not touch RocksDB, Gateway, or unrelated dirty files.
- [ ] 2.3 Implement recovery that validates both manifest slots and their referenced Raft/snapshot files, selects the highest complete pair, falls back only to a complete prior generation, and fails closed for established storage with no valid generation; verify using truncation, byte-flip, missing-file, mismatched-generation, and torn-newest-generation unit cases, and restrict edits to Persister and its tests.
- [ ] 2.4 Implement one-way reading and first-write migration for valid legacy `raftstatePersist*`/`snapshotPersist*` files plus explicit rejection of ambiguous established empty/corrupt files; verify legacy fixtures retain term, vote, logs, snapshot index, and snapshot bytes after migration, and preserve the original fixtures/backups and unrelated files.
- [ ] 2.5 Add a version, length, and checksum envelope to `PersistentTimestampOracle` while retaining legacy decimal-limit reads and existing write-sync-rename-directory-sync order; verify legacy migration, restart monotonicity, checksum rejection, and segment-boundary behavior in focused tests, and limit edits to timestamp-oracle and test files.
- [ ] 2.6 Propagate Persister/read-decode failures as fatal member startup or fatal stable-state write failures, and ensure `RequestVote`, `AppendEntries`, and `InstallSnapshot` never return safety-relevant success first; verify with RPC-order/fault tests and process exit assertions, and limit edits to Raft/persistence/RPC test paths without modifying service semantics elsewhere.

## 3. Snapshot and Readiness Implementation

- [ ] 3.1 Refactor `Raft` to track committed, dispatched, and state-machine-applied indices plus a pending snapshot and current-term leadership barrier; verify status invariants and that a received snapshot does not advance `lastApplied` before acknowledgement, and limit edits to shared Raft headers/source and focused tests.
- [ ] 3.2 Replace `CondInstallSnapshot` with explicit monotonic state-machine acknowledgement/failure APIs and serialize snapshot-before-log dispatch on the existing apply queue; verify duplicate/stale snapshots are idempotent and committed `N+1..N+K` are dispatched exactly once after snapshot `N`, and avoid adding threads or touching unrelated scheduler code.
- [ ] 3.3 Change Raft startup to load one validated bundle and enqueue its snapshot before post-snapshot logs, removing independent startup snapshot ownership from upper layers; verify a restart with snapshot plus trailing log reaches the expected applied index and data, and limit edits to Raft initialization and the two state-machine consumers.
- [ ] 3.4 Update `TsoConsensusNode::ApplyLoop` to durably observe a snapshot/log high-water mark before acknowledging Raft, fail the member on apply error, and gate `Next`, `Peek`, and `Observe` on the current-term readiness barrier; verify an unready elected Leader returns retryable failure and its first ready timestamp exceeds recovered history, and restrict edits to TSO consensus/service/status files.
- [ ] 3.5 Update `RegionPeer` snapshot and command apply paths to acknowledge only after MVCC/RocksDB restoration or mutation succeeds, reject Leader service while recovering, and remove direct `ReadSnapshot` startup installation; verify restored key/MVCC state and applied index remain monotonic, and limit edits to RegionPeer/shared Raft integration without changing NodeServer or its shared 32 RPC workers.
- [ ] 3.6 Extend Raft/TSO status with commit, dispatch, applied, pending-snapshot, leadership-barrier, storage-generation, high-water, and ready fields; verify protobuf compatibility and status JSON values during recovery and steady state, and limit edits to additive proto/status/generated-code build paths while preserving existing fields.

## 4. TSO Deadline and Failover Implementation

- [ ] 4.1 Add an optional absolute `steady_clock` deadline and recognizable timeout status to `MprpcController`; verify callers without a deadline retain existing behavior and expired/remaining-budget calculations are monotonic, and limit edits to controller and focused tests.
- [ ] 4.2 Make `MprpcChannel` honor the remaining controller budget while waiting for a pooled connection, connecting, sending, and receiving, with each phase capped by current per-attempt defaults; verify blackholed connect, partial response, connection-lock contention, and no-deadline compatibility cases, and restrict edits to MPRPC channel/controller tests.
- [ ] 4.3 Introduce immutable `TsoClientOptions` with a compatible 5-second default and refactor `Next`, `Peek`, and `Observe` to share one deadline across Leader cache, endpoint rotation, retries, and 50 ms backoff; verify no attempt/sleep starts after expiry and existing constructors/`Client::Connect` still compile, and limit edits to remote TSO client/SDK wiring and tests.
- [ ] 4.4 Preserve ambiguous `Next` semantics and idempotent `Observe` under retries, and make one shared `RemoteTimestampOracle` safe for concurrent Gateway request pthreads without a client-wide lock or new thread; verify concurrent uniqueness, legal gaps after response loss, and independent per-call deadlines under Leader change, and avoid moving blocking work onto Gateway Fibers.
- [ ] 4.5 Map startTs timeout to Begin failure before Region writes, commitTs timeout after Prewrite to rollback-before-Primary, and post-Primary-send timeout to existing primary-status resolution rather than blind rollback; verify each coordinator branch with deterministic fake TSO/Region executors, and limit edits to transaction coordinator/error mapping tests without changing unrelated 2PC behavior.
- [ ] 4.6 Add Gateway/TSO counters and latency metrics for attempts, failovers, not-leader, transport failure, ambiguous retry, and deadline exceeded; verify metric exposition after scripted success/failure calls and that logs omit timestamp/mutation payloads, and limit edits to existing metrics/status surfaces.

## 5. Fault-Domain Deployment Implementation

- [ ] 5.1 Implement the dependency-free ordered topology parser for mode plus exactly three `node_id`, endpoint, fault-domain, and data-directory records; verify valid local/distributed fixtures derive identical ascending peer order, and limit edits to a new TSO topology module and focused tests.
- [ ] 5.2 Validate duplicate/missing IDs, endpoints, directories, fault domains, unresolvable or loopback distributed endpoints, local storage writability, and persisted identity mismatch before listener/file creation; verify every invalid fixture fails with the conflicting member/field named, and restrict edits to topology/startup validation files.
- [ ] 5.3 Add `stratakv-tso --node-id N --topology <path>` while preserving the existing `--node-id --peers --state-file` local form and bind each storage directory to its stable identity; verify both CLI forms start and a changed identity refuses an existing directory, and limit edits to TSO main/topology/Persister wiring.
- [ ] 5.4 Keep `stratakv-server` defaults at `127.0.0.1:26300..26302`, label them `local-process`, add a distributed example that starts one member per host, and prevent the local launcher from claiming host-level HA; verify local start/verify still passes and distributed config dry-run emits three distinct commands, and edit only deployment scripts/examples.
- [ ] 5.5 Upgrade TSO health verification to contact configured members and require identity match, exactly one Leader, reachable majority, and ready/caught-up Leader while showing each fault domain; verify three processes with an unready Leader fail health and a ready quorum passes, and limit edits to the health command/status client paths.

## 6. Unit Tests

- [ ] 6.1 Add table-driven Persister envelope/manifest tests covering binary round trip, checksum/version/length/generation corruption, two-slot selection, retention, fresh versus established storage, and legacy migration; verify the dedicated CTest target passes repeatedly from fresh temporary directories, and edit only test/CMake plus code required by Sections 2.1-2.4.
- [ ] 6.2 Add deterministic crash-stage tests for every data/manifest/directory-sync boundary and assert restart yields the old complete bundle, new complete bundle, or explicit failure but never term zero/empty bootstrap; verify all injected stages pass in subprocesses, and keep fixtures in temporary directories without altering repository data.
- [ ] 6.3 Add shared Raft apply-protocol tests for early status, acknowledgement, duplicate/stale snapshot, failed apply, startup ordering, current-term barrier, and post-snapshot continuity; verify the dedicated CTest target observes each index transition, and limit edits to Raft test doubles/CMake.
- [ ] 6.4 Add timestamp-oracle and remote-client tests for segment restart, state corruption, absolute-deadline arithmetic, endpoint rotation, ambiguous response, idempotent Observe, connection-lock contention, and concurrent shared-client calls; verify bounded wall-clock tolerances and ThreadSanitizer when available, and confine edits to focused tests/build options without changing global toolchain defaults.
- [ ] 6.5 Add topology parser/validation/status tests for all accepted and rejected local/distributed fixtures and stable identity across restart; verify the dedicated CTest target is deterministic without external DNS/network dependencies, and limit edits to topology test fixtures/CMake.

## 7. Integration and Failure Tests

- [ ] 7.1 Extend `stratakv-test-tso` to stop a follower, allocate beyond both the 256-entry snapshot trigger and retained-log boundary, restart it, verify snapshot/state-machine catch-up plus trailing-log continuity, elect it Leader, and assert its first timestamp exceeds the recorded maximum; verify the case passes repeatedly under its CTest timeout, and edit only TSO integration/test wiring plus required implementation.
- [ ] 7.2 Add a three-peer Region integration case that forces snapshot-only follower recovery, validates MVCC/RocksDB keys and applied index, then serves from that peer after leadership change; verify reads and subsequent writes preserve snapshot contents, and limit edits to Region integration fixtures/CMake without creating per-Region RpcProviders.
- [ ] 7.3 Add SIGKILL/power-loss simulations across Raft and TSO reservation writes, full-cluster restart, persisted corruption, and one/two-member loss; verify one loss preserves strictly increasing allocation, two losses refuse allocation, and corruption fails closed, and use isolated temporary data paths only.
- [ ] 7.4 Add timeout/transaction integration cases for three blackhole endpoints, cached-Leader kill, all endpoints down, startTs timeout, commitTs timeout before Primary Commit, and ambiguous Primary Commit resolution; verify each call completes within configured deadline plus documented scheduler tolerance and leaves no incorrectly committed/rolled-back transaction, and limit edits to reliability/integration tests and required error plumbing.
- [ ] 7.5 Run topology integration with three isolated local processes labeled as simulated domains and execute the documented real-three-host checklist when an environment is available; verify identity, one-Leader readiness, single-domain survival, and two-domain refusal in captured output, and do not present loopback simulation as proof of physical independence.
- [ ] 7.6 Run the complete configured build and CTest suite, then inspect process logs, temporary-file cleanup, and repository diff for cross-module regressions; verify all tests pass with no leaked TSO/Region processes and no changes outside this change's named files/pre-existing dirty inventory.

## 8. Performance Comparison

- [ ] 8.1 Re-run the exact baseline TSO workload after implementation with identical hardware, build flags, filesystem, durability settings, segment size, request count, and concurrency; verify raw output contains throughput and p50/p95/p99 and store it beside the baseline under the change evidence directory without editing unrelated benchmark results.
- [ ] 8.2 Measure failure-mode Gateway pthread occupancy and client wall-clock time for cached-Leader loss and all endpoints blackholed before/after the deadline change; verify the post-change maximum is bounded by the configured deadline plus scheduler tolerance and record request-executor queue/active-worker metrics, limiting edits to evidence files after the benchmark code is stable.
- [ ] 8.3 Compare before/after correctness, throughput, p50/p95/p99, fsync count, disk bytes, and retained-generation space; verify every delta is computed from attached raw runs and explain/optimize any material regression without disabling sync/checksum safety, changing unrelated performance code, or using a relaxed durability mode.

## 9. Documentation, Rollout, and Final Validation

- [ ] 9.1 Write the optimization record with the original failure mechanism, safety invariants, class/instance/thread ownership, implementation decisions, before/after table, test evidence, and known trade-offs; verify every number links to raw evidence, and update `docs/优化记录` only as an explicit separate-repository change while preserving its existing dirty state.
- [ ] 9.2 Update deployment and reliability documentation for local-process versus three-fault-domain mode, topology fields, readiness health checks, 5-second default deadline, metrics, backup, rolling upgrade, and one-member-at-a-time rollback; verify every documented command with `--help`, parser dry-run, or a local smoke run, and restrict edits to deployment/reliability docs and examples.
- [ ] 9.3 Add an offline validation/export or documented backup-restore procedure that blocks direct downgrade after new-format publication; verify it against copied legacy/new-format fixtures without touching live `run_data`, and limit edits to the migration tool/runbook/test fixtures.
- [ ] 9.4 Run `openspec validate harden-tso-snapshot-durability --strict`, the full build/CTest suite, reliability cases, and performance comparison, then audit `git diff --check` plus both root and `docs/` repository status; verify all artifacts and evidence are complete and no unrelated dirty file was staged, reverted, cleaned, or reformatted.
